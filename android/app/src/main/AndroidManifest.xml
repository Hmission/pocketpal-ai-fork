<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Allow app to check if any other app can handle URLs -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
<uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- 夜间长任务前台服务（ONDEVICE_VIDEO_GEN_ANALYSIS §7.1）：防 Doze/内存配额杀进程 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- 夜间长任务电池/Doze 豁免引导（§7.1 策略 3）：发起系统弹窗请用户忽略电池优化 -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Needed only for devices running Android 9 (API 28) or lower -->
    <!-- Currently used for storing chat sessions (/Download) -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
android:requestLegacyExternalStorage="true"
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:appCategory="game"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="true"
      android:dataExtractionRules="@xml/backup_rules_12_plus"
      android:fullBackupContent="@xml/backup_rules_legacy"
      android:networkSecurityConfig="@xml/network_security_config"
      android:theme="@style/AppTheme"
      android:supportsRtl="true">
      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:hardwareAccelerated="false"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode|density"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustResize"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
        <!-- Host-scoped deep link for the Hugging Face "Use this model" flow.
             Scoped to host=hub so this is not a bare-scheme handler. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="pocketpal" android:host="hub" />
        </intent-filter>
        <!-- Checkout callback for the in-app PalsHub purchase flow. Scoped to
             host=checkout so it is routed to AuthSessionModule, not the
             hub deep-link path. -->
        <intent-filter>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.DEFAULT" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="pocketpal" android:host="checkout" />
        </intent-filter>
      </activity>

        <!-- 读屏围观（SCREENWATCH_SPEC v1）：只读无障碍服务，不执行任何写操作 -->
        <service
            android:name=".ScreenReaderService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_screen_reader" />
        </service>

        <!-- 夜间长任务前台服务（ONDEVICE_VIDEO_GEN_ANALYSIS §7.1）：
             常驻通知 + PARTIAL_WAKE_LOCK，任务进行中防 Doze/内存配额杀进程 -->
        <service
            android:name=".VideoTaskService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Optional native library for OpenCL (GPU acceleration) -->
        <uses-native-library
            android:name="libOpenCL.so"
            android:required="false" />

        <!-- Optional native library for Hexagon DSP (NPU acceleration) -->
        <uses-native-library
            android:name="libcdsprpc.so"
            android:required="false" />
    </application>

</manifest>
